# Member Flow

Tenant members are reusable contributor records. Event members are event-specific participation records.

Mobile users can create a member, optionally add an initial pledge, or attach an existing tenant member to the current event. Duplicate active phone numbers in the tenant are surfaced as conflicts so operators can attach the existing member instead of creating another record.
