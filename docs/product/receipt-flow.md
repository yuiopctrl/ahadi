# Receipt Flow

Receipts are generated atomically with payments and are never reused or deleted. Receipt detail includes tenant, event, member, payment, allocation, outstanding balance and reversal status.

Printing uses the browser print flow in this phase. Application navigation is hidden by print CSS.
